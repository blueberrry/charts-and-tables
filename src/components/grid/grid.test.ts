import React from 'react';
import { render, screen } from '@testing-library/react';
import { Grid } from './grid';

test('renders a grid with x columns', () => {
  // render(<PieChart data={}/>);
  // expect(linkElement).toBeInTheDocument();
});
