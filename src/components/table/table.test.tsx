import React from 'react';
import { render, screen } from '@testing-library/react';
import { Table } from './table';

test('renders a table when passed data', () => {
  // render(<PieChart data={}/>);
  // expect(linkElement).toBeInTheDocument();
});
