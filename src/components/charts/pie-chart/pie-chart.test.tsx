import React from 'react';
import { render, screen } from '@testing-library/react';
import { PieChart } from './pie-chart';

test('renders a pie chart when passed data', () => {
  // render(<PieChart data={}/>);
  // expect(linkElement).toBeInTheDocument();
});
